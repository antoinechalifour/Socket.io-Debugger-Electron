import styled from 'styled-components'

const Button = styled.button`
  background: none;
  border: none;
  font-family: inherit;
  font-size: inherit;
  background: #373d3f;
  color: #fff;
  border-radius: 2px;
`

export default Button
